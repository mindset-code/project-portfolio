node_modules/
dist/
.env.local
.env.*.local
.env.personal
CLAUDE.md
CLAUDE.local.md
.claude/
agents/
skills/

# Engram local memory
.engram/
dist-marca/
dist-personal/
.firebase/
firebase.personal.json

# Lo que solo existe en la maquina que compila la variante firmada. La
# fotografia del titular vive AQUI y no en public/: Vite copia public/
# entera a la salida, asi que ahi se publicaria tambien en la variante de
# marca, que es justo la que no debe llevar cara (ver src/identidad.js).
personal/
firebase-debug.log
